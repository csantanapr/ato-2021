{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "production": "node src/index.js",
    "start": "npm run production",
    "development": "nodemon src/index.js",
    "setup": "skaffold config set default-repo dev.local",
    "dev": "npm run dev-knative",
    "url": "kubectl get ksvc node -o jsonpath='{.status.url}\n'",
    "run": "skaffold run -p knative",
    "kube-debug": "skaffold debug --port-forward",
    "kube-dev": "skaffold dev --port-forward",
    "dev-knative": "skaffold dev -p dev-knative"
  },
  "dependencies": {
    "express": "^4.16.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.3"
  },
  "license": "Apache-2.0"
}
